apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: prestos.prestocontroller.prestodb.io
spec:
  group: prestocontroller.prestodb.io
  version: v1alpha1
  names:
    kind: Presto
    singular: presto
    plural: prestos
  scope: Namespaced
  validation:
    openAPIV3Schema:
      properties:
        spec:
          properties:
            workers:
              type: integer
              minimum: 1
              maximum: 10